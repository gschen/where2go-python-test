'''
使用pip命令安装NumPy后做如下操作，
import numpy as np
c=np.mat(np.arange(9).reshape(3,3))
print(c)
（1）	在numpy库中，哪个函数产生矩阵
（2）	reshap函数的作用是
'''
#安装  pip install numpy
import numpy as np
c = np.mat(np.arange(9).reshape(3,3))
print(c)
#(1)  np.matlip库可以产生矩阵
#(2)  重新确定数组形状